apiVersion: v1
kind: Service
metadata:
  name: ai-conversation-studio-service
  namespace: ai-conversation-studio
  labels:
    app: ai-conversation-studio
spec:
  type: LoadBalancer
  selector:
    app: ai-conversation-studio
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http

